# 王勇顺的 XMUOJ 题解站

作者：天帝——王勇顺

这是一个离线静态题解站，整理了本目录中的 `李胜睿剑道试炼` 和 `李胜睿例题` 代码，支持题目搜索、题面速览、代码高亮和一键复制。

## 本地预览

直接打开 `index.html`，或进入 `site/index.html`。

## 更新数据

修改题解源码后，在仓库根目录运行：

```powershell
python generate_site.py
```

生成器会重新扫描 `李胜睿剑道试炼` 和 `李胜睿例题`，并更新 `site/data.js`。
